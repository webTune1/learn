<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Learn HTML, CSS, JS, Python</title>
  <style>
    :root {
      --bg-light: #f9f9f9;
      --text-light: #222;
      --bg-dark: #1e1e1e;
      --text-dark: #eee;
      --accent: #007acc;
    }

    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: var(--bg-light);
      color: var(--text-light);
      transition: 0.3s;
    }

    header {
      background-color: var(--accent);
      color: white;
      padding: 20px;
      text-align: center;
    }

    nav {
      display: flex;
      justify-content: center;
      gap: 15px;
      background-color: #ddd;
      padding: 10px;
    }

    nav button {
      padding: 10px 15px;
      border: none;
      background-color: var(--accent);
      color: white;
      cursor: pointer;
      border-radius: 5px;
    }

    section {
      padding: 20px;
      display: none;
    }

    .active {
      display: block;
    }

    .theme-toggle {
      position: fixed;
      top: 10px;
      right: 10px;
      padding: 8px;
      background: var(--accent);
      color: white;
      border: none;
      cursor: pointer;
      border-radius: 5px;
    }

    pre {
      background: #eee;
      padding: 10px;
      overflow-x: auto;
      border-radius: 5px;
    }

    .dark-mode {
      background-color: var(--bg-dark);
      color: var(--text-dark);
    }

    .dark-mode nav {
      background-color: #333;
    }

    .dark-mode pre {
      background-color: #2e2e2e;
      color: #ccc;
    }
  </style>
</head>
<body>
  <header>
    <h1>📘 Learn HTML, CSS, JavaScript, Python</h1>
    <p>Click a topic below to start learning</p>
  </header>

  <nav>
    <button onclick="showSection('html')">HTML</button>
    <button onclick="showSection('css')">CSS</button>
    <button onclick="showSection('js')">JavaScript</button>
    <button onclick="showSection('python')">Python</button>
  </nav>

  <button class="theme-toggle" onclick="toggleTheme()">Toggle Theme</button>

  <section id="html" class="active">
    <h2>🧱 HTML Basics</h2>
    <p>HTML creates the structure of a webpage.</p>
    <pre>
&lt;!DOCTYPE html&gt;
&lt;html&gt;
  &lt;head&gt;&lt;title&gt;My Page&lt;/title&gt;&lt;/head&gt;
  &lt;body&gt;
    &lt;h1&gt;Welcome!&lt;/h1&gt;
    &lt;p&gt;This is a paragraph.&lt;/p&gt;
  &lt;/body&gt;
&lt;/html&gt;
    </pre>
    <h3>Mini Project</h3>
    <p>Create a profile page with your name, photo, and hobbies.</p>
  </section>

  <section id="css">
    <h2>🎨 CSS Basics</h2>
    <p>CSS styles your webpage: colors, layout, fonts.</p>
    <pre>
body {
  background-color: lightblue;
  font-family: Arial;
}
h1 {
  color: darkblue;
  text-align: center;
}
    </pre>
    <h3>Mini Project</h3>
    <p>Style your profile page with colors and spacing.</p>
  </section>

  <section id="js">
    <h2>⚙️ JavaScript Basics</h2>
    <p>JavaScript makes your page interactive.</p>
    <pre>
function greet() {
  alert("Hello, welcome!");
}
    </pre>
    <h3>Mini Project</h3>
    <p>Add a button that changes text when clicked.</p>
    <pre>
&lt;button onclick="changeText()"&gt;Click Me&lt;/button&gt;
&lt;p id="demo"&gt;Original Text&lt;/p&gt;

&lt;script&gt;
function changeText() {
  document.getElementById("demo").innerHTML = "Text changed!";
}
&lt;/script&gt;
    </pre>
  </section>

  <section id="python">
    <h2>🐍 Python Basics</h2>
    <p>Python is used for automation, data, and more.</p>
    <pre>
name = input("What is your name? ")
print("Hello", name)
    </pre>
    <h3>Mini Project</h3>
    <p>Track daily egg count for your poultry farm.</p>
    <pre>
total_eggs = 0
for day in range(1, 8):
    eggs = int(input(f"Day {day} eggs: "))
    total_eggs += eggs
print("Total eggs this week:", total_eggs)
    </pre>
  </section>

  <script>
    function showSection(id) {
      document.querySelectorAll('section').forEach(sec => sec.classList.remove('active'));
      document.getElementById(id).classList.add('active');
    }

    function toggleTheme() {
      document.body.classList.toggle('dark-mode');
    }
  </script>
</body>
</html>
